# Deployment notes

- Replace the current GitHub Pages site files with this package.
- Do not delete or replace the repository's existing `CNAME` file.
- Keep the current GitHub Pages custom-domain setting and Porkbun DNS records unchanged.
- Both forms post to `https://formspree.io/f/xzepwgqk` and include distinct `form_type` values.
- After deployment, verify the live custom-domain URL at desktop and mobile widths and make one non-sensitive test submission from each form.
